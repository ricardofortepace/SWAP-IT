<div class="container-stuffs">
    <% @stuffs.each do |stuff| %>
      <%= link_to stuff do %>
          <div class="imagem ">
            <%= cl_image_tag(stuff.photos.first.key, height: 200, width: 200, crop: :fill)%>
            <p class="detail"><%=stuff.name%></p>
        </div>
      <%end%>
    <% end %>
</div>


  <div class="container p-5">
    <% @chatrooms.each do |chatroom| %>
    <div><%= link_to "go to chat:#{chatroom.stuff.name} X #{chatroom.trading_stuff.name}", chatroom_path(chatroom), class:"m-5" %></div>
    <% end %>
  </div>


